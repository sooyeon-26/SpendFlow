# Public build flags. These contain no secrets.
VITE_ENABLE_SPEND_ALERTS=false
VITE_ENABLE_DAILY_REPORTS=false

# Server-only values. Configure these in the deployment environment.
# Never prefix webhook URLs with VITE_.
N8N_SPEND_ALERT_WEBHOOK_URL=
N8N_DAILY_REPORT_WEBHOOK_URL=
